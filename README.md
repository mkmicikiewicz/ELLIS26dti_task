## git lfs installation 
To download the data you have to have git lfs installed. With no sudo access you can do it with conda:
```
conda install -c conda-forge git-lfs
```
Then run (once per machine):
```
git lfs install
```
Now, clone the repo and pull the data
